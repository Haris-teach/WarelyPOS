import React, { Component } from "react";
import { Text, View } from "react-native";

const Shift =()=>{
  return(
    <View>
      <Text>Shift</Text>
    </View>
  );
}

export default Shift;
